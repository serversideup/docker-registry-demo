# Docker Registry Configuration
# Copy this file to .env and update with your actual values:
#   cp env.example .env

# Your registry domain name (must have DNS A record pointing to this server)
REGISTRY_DOMAIN=registry.example.com

